import styled from "@emotion/styled"

const Row = styled.div`
  display: flex;
  justify-content: space-between;
  width: 100%;
  padding: ${({ theme }) => theme.space.spacing2} 0;
`
export default Row
